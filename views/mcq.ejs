<!-- mcq.ejs -->

<!DOCTYPE html>
<html lang="en">
<head>
  <style>
    .question-box {
        border: 2px solid #ccc; /* Add a border */
        padding: 10px; /* Add padding */
        margin-bottom: 20px; /* Add margin bottom for spacing between questions */
    }

    .button-container {
            text-align: center;
        }

        /* Style for the button */
        button {
            background-color: #4BB7E6; /* Background color */
            color: rgb(0, 0, 0); /* Text color */
            padding: 20px 40px; /* Padding (vertical, horizontal) */
            font-size: 20px; /* Font size */
            border: none; /* Remove border */
            cursor: pointer; /* Add cursor pointer */
            border-radius: 10px; /* Border radius */
        }
        h1 {
    margin-top: 0;
  font-size: 2.6em;
  margin-bottom: 0.5em;
  text-decoration: underline;
}
</style>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Neuro Quiz</title>
</head>
<body>
  <h1 style="display: flex; align-items: center; text-align: center; justify-content: center; text-decoration: underline;"><b>🧠🧪Neuro Quiz📚👨‍🔬</b></h1>

  <form action="/submit" method="POST">
    <ol>
      <% for (let item of items) { %>
        <li class="question-box">
          <h3><%= item.question %></h3>
          <ul>
            <li style=" border: 1px solid #0a15e8;background-color: #c1d3ef;"><label><input type="radio" name="<%= item.id %>" value="a=<%= item.answer%>">  <%= item['option-1'] %></label></li>
            <li style=" border: 1px solid #0a15e8;background-color: #e5e9f0;"><label><input type="radio" name="<%= item.id %>" value="b=<%= item.answer%>">  <%= item['option-2'] %></label></li>
            <li style=" border: 1px solid #0a15e8;background-color: #c1d3ef;"><label><input type="radio" name="<%= item.id %>" value="c=<%= item.answer%>">  <%= item['option-3'] %></label></li>
            <li style=" border: 1px solid #0a15e8;background-color: #e5e9f0;"><label><input type="radio" name="<%= item.id %>" value="d=<%= item.answer%>">  <%= item['option-4'] %></label></li>
          </ul>
        </li>
      <% }; %>
    </ol>
    <div class="button-container">
      <button type="submit"><b>Submit</b></button>
  </div>
  </form>

</body>
</html>
